@using StFrancisChurch.Models.Utility
@{
    var parishEvent = (ParishEventUI)ViewBag.parishEvent;
}

<div class="container-wrap">
    <div id="fh5co-intro">
        <div class="row animate-box">
            <div class="col-md-12 col-md-offset-0 text-center">
                <h2>@parishEvent.Name</h2>
                <span>@parishEvent.Description</span>
            </div>
        </div>
    </div>
</div>